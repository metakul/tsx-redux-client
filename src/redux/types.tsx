// import { UserData } from "../interfaces/interface";
// import rootReducer from './rootReducer';

// export type Action =
// | { type: 'LOGIN_PENDING' }
// | { type: 'LOGIN_FULFILLED' } // TODO: add jwt with payload
// | { type: 'LOGIN_REJECTED' }
// | { type: 'USER_PENDING' }
// | { type: 'USER_FULFILLED'; payload: { data: UserData } }
// | { type: 'USER_REJECTED' };

// export type LoginRootState = ReturnType<typeof rootReducer>;
